productPrice = 300;
muru = "nije giye niye asho jaw";

freeShiping =  (productPrice>=100)&&(6>9) ? true : muru;

console.log(freeShiping)


one=1;
own="1"
cond = !(one === own) ? 'go' : 'to';
console.log(cond)